---
layout: layouts/base.njk
section: post
---

{% include "components/pembatas.njk" %}

<section class="games-single-page">
    <div class="container">
        <div class="game-single-preview">
            <img src="{{ '/' | url }}_includes/assets/img/games/big.jpg" alt="">
        </div>
        <div class="row">
            <div class="col-xl-9 col-lg-8 col-md-7 game-single-content">
                <h2 class="gs-title">{{ title }}</h2>
                    <p>
                        <small>
                        <time datetime="{{ date | machineDate }}">{{ date | readableDate }}</time> by {{ metadata.author.name }}
                        </small>
                    </p>
                <h4>Gameplay</h4>
                {{ layoutContent | safe }}
                    {% set bukan = ['tech','post','politics','environment','sport','automotive'] %}
                </p>
                <div class="geme-social-share pt-5 d-flex">
                    <p>Share:</p>
                    <a href="#"><i class="fa fa-pinterest"></i></a>
                    <a href="#"><i class="fa fa-facebook"></i></a>
                    <a href="#"><i class="fa fa-twitter"></i></a>
                    <a href="#"><i class="fa fa-dribbble"></i></a>
                    <a href="#"><i class="fa fa-behance"></i></a>
                </div>
                <nav>
                {% if tags %}
                    {% for tag in tags %}
                        {% if tag not in bukan %}
                            <a href="{{ site.url }}/{{ tag | slug }}/">← {{ tag }} index</a>
                                {%- endif -%}
                    {% endfor %}
                {% endif %}
                </nav>
            </div>
        </div>
    </div>
</section>






